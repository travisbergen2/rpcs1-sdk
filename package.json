{
  "name": "rpcs1-sdk",
  "version": "0.2.1",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build:web",
    "build:web": "npm run build --workspace=packages/core && npm run build --workspace=packages/web",
    "build:all": "npm run build --workspace=packages/core && npm run build --workspace=packages/mcp-server && npm run build --workspace=packages/web",
    "dev": "npm run dev --workspace=packages/web"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=20 <27"
  }
}
