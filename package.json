{
  "name": "rpcs1-sdk",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=packages/web",
    "dev": "npm run dev --workspace=packages/web"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": "20.x"
  }
}
