{
  "name": "@rpcs1/extension",
  "version": "0.7.0",
  "private": true,
  "description": "Explicit Formula browser extension: underlines words a reader could take two ways, before you send. Tap it, pick what you meant, or say it in your own words. (Store-facing name changed from RPCS-1 Ambiguity Check in 0.7.0; internal identifiers keep the rpcs1 prefix.)",
  "scripts": {
    "test": "node --test",
    "pack": "node scripts/pack.mjs"
  }
}
