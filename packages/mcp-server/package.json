{
  "name": "@rpcs1/mcp-server",
  "version": "0.2.1",
  "description": "Standalone STDIO MCP server for RPCS1 Agent Tuner",
  "license": "MIT",
  "type": "module",
  "bin": {
    "rpcs1-mcp-server": "./dist/index.js"
  },
  "scripts": {
    "build": "node -e \"require('fs').rmSync('dist', { recursive: true, force: true })\" && tsup ././src/index.ts --format esm --platform node --target node20 --out-dir dist",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@rpcs1/core": "*",
    "zod": "^3.23.6"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.4.0"
  },
  "optionalDependencies": {
    "@rollup/rollup-linux-x64-gnu": "^4.60.4"
  }
}
