{
  "name": "@rpcs1/obsidian-plugin",
  "version": "0.7.0",
  "description": "Explicit Formula - The Loop, inside Obsidian (panel + vault priors + graph-visible write-backs). Canonical source; the community-directory repo is a mirror built by scripts/mirror.mjs.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "test": "vitest run --config ./vitest.config.ts",
    "lint": "tsc --noEmit",
    "mirror": "node scripts/mirror.mjs"
  },
  "devDependencies": {
    "@types/node": "^20",
    "esbuild": "^0.27.7",
    "obsidian": "latest",
    "typescript": "^5.4.0",
    "vitest": "^4.1.8",
    "jszip": "^3.10.1"
  }
}
