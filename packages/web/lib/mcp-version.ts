// Single source of truth for the MCP server's advertised version.
// serverInfo (lib/mcp-server.ts), /api/health, and generated profile docs
// all read this constant — bump it here only.
export const MCP_SERVER_VERSION = '0.4.2';
